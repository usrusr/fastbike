package de.ulf_schreiber.fastbike.boundingpiecechain;

public class HeightTest {
    static class Tree extends SimpleTreeHeight {

        public Tree(int blocksize, double precision) {
            super(blocksize, precision);
        }
    }
}
